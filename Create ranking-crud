spring:
  application:
    name: ranking-crud

  datasource:
    url: jdbc:postgresql://localhost:5432/db-ranking
    username: postgres
    password: postgres

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
